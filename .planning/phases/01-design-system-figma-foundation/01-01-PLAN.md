---
phase: 01-design-system-figma-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/avatar.tsx
  - src/components/ui/shared.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(dashboard)/objectives/page.tsx
  - src/app/(dashboard)/standards/page.tsx
  - src/app/(dashboard)/definitions/page.tsx
  - src/app/(dashboard)/settings/page.tsx
  - src/app/(dashboard)/profiles/page.tsx
  - src/app/(dashboard)/profiles/[id]/page.tsx
  - src/app/(dashboard)/profiles/[id]/train/page.tsx
  - src/app/(dashboard)/chat/page.tsx
  - src/app/(dashboard)/outputs/page.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements:
  - DSGN-05

must_haves:
  truths:
    - "shared.tsx no longer exists in src/components/ui/"
    - "Each of the 6 component groups (Card, Textarea, Badge, Separator, ScrollArea, Avatar) lives in its own file under src/components/ui/"
    - "All 12 consumer files import from the new individual files, not from shared"
    - "TypeScript compiles with zero errors (npx tsc --noEmit)"
    - "The application renders identically before and after the split (no visual change)"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent"
      exports: ["Card", "CardHeader", "CardFooter", "CardTitle", "CardDescription", "CardContent"]
    - path: "src/components/ui/textarea.tsx"
      provides: "Textarea component and TextareaProps interface"
      exports: ["Textarea", "TextareaProps"]
    - path: "src/components/ui/badge.tsx"
      provides: "Badge component with variant system"
      exports: ["Badge", "badgeVariants"]
    - path: "src/components/ui/separator.tsx"
      provides: "Separator component wrapping Radix Separator"
      exports: ["Separator"]
    - path: "src/components/ui/scroll-area.tsx"
      provides: "ScrollArea component wrapping Radix ScrollArea"
      exports: ["ScrollArea"]
    - path: "src/components/ui/avatar.tsx"
      provides: "Avatar and AvatarFallback components wrapping Radix Avatar"
      exports: ["Avatar", "AvatarFallback"]
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/ui/card.tsx"
      via: "import statement"
      pattern: "from \"@/components/ui/card\""
    - from: "src/app/(dashboard)/chat/page.tsx"
      to: "src/components/ui/badge.tsx"
      via: "import statement"
      pattern: "from \"@/components/ui/badge\""
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/components/ui/scroll-area.tsx"
      via: "import statement"
      pattern: "from \"@/components/ui/scroll-area\""
---

<objective>
Split the monolithic shared.tsx into individual component files following shadcn/UI convention (one component group per file), and update all 12 consumer files to import from the new paths.

Purpose: Establish the correct file structure for the shadcn/UI component system before any visual changes. This is a structural refactor with zero visual impact -- every component renders identically after the split.

Output: 6 new component files in src/components/ui/, shared.tsx deleted, all imports updated, TypeScript compiling cleanly.
</objective>

<execution_context>
@/Users/thegoodmachine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thegoodmachine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-figma-foundation/01-RESEARCH.md

@src/components/ui/shared.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 individual component files from shared.tsx</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/textarea.tsx
    src/components/ui/badge.tsx
    src/components/ui/separator.tsx
    src/components/ui/scroll-area.tsx
    src/components/ui/avatar.tsx
  </files>
  <action>
    Split shared.tsx into 6 individual files. Each file gets:
    1. Its own React import (`import * as React from "react"`)
    2. Its own cn import (`import { cn } from "@/lib/utils"`)
    3. Any Radix imports it needs (separator needs @radix-ui/react-separator, scroll-area needs @radix-ui/react-scroll-area, avatar needs @radix-ui/react-avatar)
    4. The exact component code from shared.tsx (copy verbatim, do NOT modify any className, prop, or behavior)
    5. Named exports at the bottom

    File-by-file:

    **card.tsx**: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter. Imports: React, cn. Export all 6.

    **textarea.tsx**: TextareaProps interface and Textarea component. Imports: React, cn. Export TextareaProps and Textarea.

    **badge.tsx**: badgeVariants object, BadgeProps interface, Badge function component. Imports: React, cn. Export Badge (and optionally badgeVariants for reuse).

    **separator.tsx**: Separator component using @radix-ui/react-separator. Imports: React, cn, SeparatorPrimitive. Export Separator.

    **scroll-area.tsx**: ScrollArea component using @radix-ui/react-scroll-area. Imports: React, cn, ScrollAreaPrimitive. Export ScrollArea.

    **avatar.tsx**: Avatar and AvatarFallback components using @radix-ui/react-avatar. Imports: React, cn, AvatarPrimitive. Export Avatar, AvatarFallback.

    IMPORTANT: Copy the component code EXACTLY from shared.tsx. Do not "improve" or modify any styling, prop handling, or behavior. This is a move, not a refactor.
  </action>
  <verify>
    All 6 files exist in src/components/ui/:
    ```bash
    ls src/components/ui/card.tsx src/components/ui/textarea.tsx src/components/ui/badge.tsx src/components/ui/separator.tsx src/components/ui/scroll-area.tsx src/components/ui/avatar.tsx
    ```
  </verify>
  <done>6 individual component files exist, each containing the exact component code from shared.tsx with correct imports and exports.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 12 consumer files and delete shared.tsx</name>
  <files>
    src/app/(auth)/login/page.tsx
    src/app/(auth)/register/page.tsx
    src/app/(dashboard)/objectives/page.tsx
    src/app/(dashboard)/standards/page.tsx
    src/app/(dashboard)/definitions/page.tsx
    src/app/(dashboard)/settings/page.tsx
    src/app/(dashboard)/profiles/page.tsx
    src/app/(dashboard)/profiles/[id]/page.tsx
    src/app/(dashboard)/profiles/[id]/train/page.tsx
    src/app/(dashboard)/chat/page.tsx
    src/app/(dashboard)/outputs/page.tsx
    src/components/layout/app-sidebar.tsx
    src/components/ui/shared.tsx
  </files>
  <action>
    Update every file that imports from `@/components/ui/shared` to import from the new individual files. Use this exact import map:

    | File | Old Import | New Import(s) |
    |------|-----------|---------------|
    | login/page.tsx | `{ Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle }` from shared | same from `@/components/ui/card` |
    | register/page.tsx | `{ Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle }` from shared | same from `@/components/ui/card` |
    | objectives/page.tsx | `{ Card, CardContent, Badge, Textarea }` from shared | `{ Card, CardContent }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge`, `{ Textarea }` from `@/components/ui/textarea` |
    | standards/page.tsx | `{ Card, CardContent, Badge }` from shared | `{ Card, CardContent }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge` |
    | definitions/page.tsx | `{ Card, CardContent }` from shared | same from `@/components/ui/card` |
    | settings/page.tsx | `{ Card, CardContent, CardHeader, CardTitle, CardDescription, Separator, Badge }` from shared | `{ Card, CardContent, CardHeader, CardTitle, CardDescription }` from `@/components/ui/card`, `{ Separator }` from `@/components/ui/separator`, `{ Badge }` from `@/components/ui/badge` |
    | profiles/page.tsx | `{ Card, CardContent, CardHeader, CardTitle, Badge }` from shared | `{ Card, CardContent, CardHeader, CardTitle }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge` |
    | profiles/[id]/page.tsx | `{ Card, CardContent, CardHeader, CardTitle, CardDescription, Badge, Separator }` from shared | `{ Card, CardContent, CardHeader, CardTitle, CardDescription }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge`, `{ Separator }` from `@/components/ui/separator` |
    | profiles/[id]/train/page.tsx | `{ Badge, Card, CardContent, CardHeader, CardTitle }` from shared | `{ Card, CardContent, CardHeader, CardTitle }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge` |
    | chat/page.tsx | `{ Badge }` from shared | `{ Badge }` from `@/components/ui/badge` |
    | outputs/page.tsx | `{ Card, CardContent, Badge }` from shared | `{ Card, CardContent }` from `@/components/ui/card`, `{ Badge }` from `@/components/ui/badge` |
    | app-sidebar.tsx | `{ ScrollArea }` from shared | `{ ScrollArea }` from `@/components/ui/scroll-area` |

    IMPORTANT: Read each file first to verify the exact imports before modifying. The import map above is from research -- verify against actual file contents.

    After ALL imports are updated, DELETE shared.tsx entirely.

    Then run `npx tsc --noEmit` to verify zero TypeScript errors. If any errors appear, they indicate a missed import -- fix before proceeding.
  </action>
  <verify>
    ```bash
    # Verify shared.tsx is gone
    test ! -f src/components/ui/shared.tsx && echo "DELETED" || echo "STILL EXISTS"

    # Verify no remaining imports from shared
    grep -r "from.*@/components/ui/shared" src/ && echo "BROKEN IMPORTS FOUND" || echo "ALL CLEAN"

    # TypeScript check
    npx tsc --noEmit
    ```
  </verify>
  <done>shared.tsx is deleted, all 12 consumer files import from individual component files, `npx tsc --noEmit` passes with zero errors, and `grep` finds no remaining references to `@/components/ui/shared`.</done>
</task>

</tasks>

<verification>
1. `ls src/components/ui/` shows: avatar.tsx, badge.tsx, button.tsx, card.tsx, input.tsx, label.tsx, scroll-area.tsx, separator.tsx, textarea.tsx (no shared.tsx)
2. `grep -r "from.*@/components/ui/shared" src/` returns no results
3. `npx tsc --noEmit` exits with code 0
4. `npm run build` completes successfully (optional but recommended)
</verification>

<success_criteria>
- shared.tsx does not exist
- 6 new component files exist in src/components/ui/ with exact content from the original shared.tsx
- All 12 consumer files have updated imports pointing to individual component files
- TypeScript compilation passes with zero errors
- No visual changes to any page (structural refactor only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-figma-foundation/01-01-SUMMARY.md`
</output>
