---
phase: 02-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/select.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/breadcrumb.tsx
  - src/components/ui/table.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/form.tsx
  - src/components/ui/popover.tsx
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - "All 12 required shadcn/UI primitives are installed and importable from src/components/ui/"
    - "Each primitive renders with the Phase 1 design tokens (indigo/zinc palette, Geist font, border-radius)"
    - "react-hook-form and @hookform/resolvers are available as dependencies for form validation integration"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogTrigger, DialogClose"
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem"
    - path: "src/components/ui/select.tsx"
      provides: "Select, SelectTrigger, SelectValue, SelectContent, SelectItem"
    - path: "src/components/ui/tabs.tsx"
      provides: "Tabs, TabsList, TabsTrigger, TabsContent"
    - path: "src/components/ui/tooltip.tsx"
      provides: "Tooltip, TooltipTrigger, TooltipContent, TooltipProvider"
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton primitive with pulse animation"
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent"
    - path: "src/components/ui/breadcrumb.tsx"
      provides: "Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbSeparator"
    - path: "src/components/ui/table.tsx"
      provides: "Table, TableHeader, TableBody, TableRow, TableHead, TableCell"
    - path: "src/components/ui/sheet.tsx"
      provides: "Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle"
    - path: "src/components/ui/form.tsx"
      provides: "Form, FormField, FormItem, FormLabel, FormControl, FormMessage, FormDescription"
    - path: "src/components/ui/popover.tsx"
      provides: "Popover, PopoverTrigger, PopoverContent"
  key_links:
    - from: "src/components/ui/form.tsx"
      to: "react-hook-form"
      via: "useFormContext from react-hook-form"
      pattern: "useFormContext|FormProvider"
    - from: "src/components/ui/skeleton.tsx"
      to: "src/app/globals.css"
      via: "Tailwind animate-pulse class using design tokens"
      pattern: "animate-pulse"
---

<objective>
Install all 12 required shadcn/UI primitives and the react-hook-form dependency.

Purpose: Establish the complete component primitive layer that every subsequent plan and phase composes into pages. Without these primitives installed, no page-level work can begin.

Output: 12 new component files in src/components/ui/, react-hook-form + @hookform/resolvers in package.json.
</objective>

<execution_context>
@/Users/thegoodmachine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thegoodmachine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components.json
@package.json
@tailwind.config.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install all 12 shadcn/UI primitives via CLI</name>
  <files>
    src/components/ui/dialog.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/select.tsx
    src/components/ui/tabs.tsx
    src/components/ui/tooltip.tsx
    src/components/ui/skeleton.tsx
    src/components/ui/alert-dialog.tsx
    src/components/ui/breadcrumb.tsx
    src/components/ui/table.tsx
    src/components/ui/sheet.tsx
    src/components/ui/form.tsx
    src/components/ui/popover.tsx
    package.json
    package-lock.json
  </files>
  <action>
    Install all 12 shadcn/UI primitives using the shadcn CLI. The project already has `components.json` configured with new-york style, RSC, and the correct aliases.

    Run the following commands (use `--yes` flag to skip prompts, `--overwrite` to handle any existing files):

    ```bash
    npx shadcn@latest add dialog --yes --overwrite
    npx shadcn@latest add dropdown-menu --yes --overwrite
    npx shadcn@latest add select --yes --overwrite
    npx shadcn@latest add tabs --yes --overwrite
    npx shadcn@latest add tooltip --yes --overwrite
    npx shadcn@latest add skeleton --yes --overwrite
    npx shadcn@latest add alert-dialog --yes --overwrite
    npx shadcn@latest add breadcrumb --yes --overwrite
    npx shadcn@latest add table --yes --overwrite
    npx shadcn@latest add sheet --yes --overwrite
    npx shadcn@latest add form --yes --overwrite
    npx shadcn@latest add popover --yes --overwrite
    ```

    The `form` component from shadcn will automatically install `react-hook-form` and `@hookform/resolvers` as dependencies. If it does not, install them manually:

    ```bash
    npm install react-hook-form @hookform/resolvers
    ```

    **IMPORTANT:** The shadcn CLI reads `components.json` which points to:
    - Style: `new-york`
    - CSS: `src/app/globals.css`
    - Components alias: `@/components`
    - Utils alias: `@/lib/utils`

    The generated components will automatically use the Phase 1 design tokens (indigo/zinc palette) because they reference CSS variables (`--primary`, `--border`, etc.) via the Tailwind theme config.

    **Note on @radix-ui/react-popover:** This Radix package is NOT currently in package.json. The shadcn CLI `add popover` command will install it automatically.

    After installation, verify all 12 files exist in `src/components/ui/` and that `package.json` includes `react-hook-form` and `@hookform/resolvers`.
  </action>
  <verify>
    1. Run `ls src/components/ui/{dialog,dropdown-menu,select,tabs,tooltip,skeleton,alert-dialog,breadcrumb,table,sheet,form,popover}.tsx` -- all 12 files must exist
    2. Run `grep "react-hook-form" package.json` -- must find a match
    3. Run `grep "@hookform/resolvers" package.json` -- must find a match
    4. Run `grep "@radix-ui/react-popover" package.json` -- must find a match (newly installed)
    5. Run `npx tsc --noEmit` -- TypeScript compilation must pass with zero errors
  </verify>
  <done>All 12 shadcn/UI primitive component files exist in src/components/ui/, react-hook-form and @hookform/resolvers are in package.json, @radix-ui/react-popover is installed, and TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Verify primitives render with design tokens</name>
  <files>
    src/components/ui/dialog.tsx
    src/components/ui/dropdown-menu.tsx
    src/components/ui/select.tsx
    src/components/ui/tabs.tsx
    src/components/ui/tooltip.tsx
    src/components/ui/skeleton.tsx
    src/components/ui/alert-dialog.tsx
    src/components/ui/breadcrumb.tsx
    src/components/ui/table.tsx
    src/components/ui/sheet.tsx
    src/components/ui/form.tsx
    src/components/ui/popover.tsx
  </files>
  <action>
    Verify each of the 12 installed component files references the design system tokens correctly. Check that:

    1. Each file imports `cn` from `@/lib/utils` (the Tailwind merge utility)
    2. Components use semantic color classes (e.g., `bg-background`, `text-foreground`, `border-border`, `bg-primary`) rather than hardcoded colors
    3. Components use the `--radius` CSS variable via Tailwind's `rounded-lg`, `rounded-md`, `rounded-sm` classes (mapped in tailwind.config.ts)
    4. No hardcoded font families -- components inherit Geist from the body's `font-sans` class

    If any component file has hardcoded colors (e.g., `bg-white`, `text-gray-500`), replace them with the semantic equivalents:
    - `bg-white` → `bg-background`
    - `text-gray-500` → `text-muted-foreground`
    - `border-gray-200` → `border-border`
    - `bg-gray-100` → `bg-muted`

    Also verify the skeleton component uses `animate-pulse` (the pulse fade animation per user decision -- NOT shimmer sweep).

    Run `npm run build` to confirm the full Next.js build succeeds with all new components.
  </action>
  <verify>
    1. Run `grep -l "cn" src/components/ui/{dialog,dropdown-menu,select,tabs,tooltip,skeleton,alert-dialog,breadcrumb,table,sheet,form,popover}.tsx | wc -l` -- should be 12 (all files use cn)
    2. Run `grep -c "bg-white\|text-gray\|border-gray\|bg-gray" src/components/ui/{dialog,dropdown-menu,select,tabs,tooltip,skeleton,alert-dialog,breadcrumb,table,sheet,form,popover}.tsx` -- should return 0 matches per file (no hardcoded colors)
    3. Run `grep "animate-pulse" src/components/ui/skeleton.tsx` -- must find pulse animation
    4. Run `npm run build` -- must exit with code 0
  </verify>
  <done>All 12 shadcn/UI primitives use semantic design token classes (no hardcoded colors), skeleton uses pulse animation, and the Next.js build passes.</done>
</task>

</tasks>

<verification>
1. `ls src/components/ui/*.tsx | wc -l` -- at least 21 files (9 existing + 12 new)
2. `npx tsc --noEmit` -- zero TypeScript errors
3. `npm run build` -- successful Next.js build
4. `grep "react-hook-form" package.json` -- dependency present
5. `grep "@hookform/resolvers" package.json` -- dependency present
6. `grep "@radix-ui/react-popover" package.json` -- dependency present
7. No hardcoded color values in any new component file
</verification>

<success_criteria>
All 12 required shadcn/UI primitives (dialog, dropdown-menu, select, tabs, tooltip, skeleton, alert-dialog, breadcrumb, table, sheet, form, popover) are installed in src/components/ui/ via the shadcn CLI, render with the Phase 1 indigo/zinc design tokens, and the project builds successfully. react-hook-form and @hookform/resolvers are installed for form validation integration in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-01-SUMMARY.md`
</output>
