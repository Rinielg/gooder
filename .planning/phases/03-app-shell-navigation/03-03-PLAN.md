---
phase: 03-app-shell-navigation
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/mobile-top-bar.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements:
  - SHELL-02

must_haves:
  truths:
    - "On viewports narrower than 1024px (lg breakpoint), sidebar renders as a slide-out Sheet drawer instead of a fixed panel"
    - "Overlay tap closes the mobile drawer"
    - "Navigating to a page does NOT auto-close the drawer"
    - "Mobile drawer has full content parity with desktop sidebar (same nav items, profile selector, sign out)"
    - "Mobile top bar shows logo/brand only (not page title) with hamburger trigger and New Chat shortcut button"
    - "Desktop sidebar is hidden on mobile viewports, mobile nav is hidden on desktop viewports"
  artifacts:
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Sheet-based mobile navigation drawer with full sidebar content parity"
      exports: ["MobileNav"]
    - path: "src/components/layout/mobile-top-bar.tsx"
      provides: "Mobile top bar with hamburger trigger, logo, and New Chat shortcut"
      exports: ["MobileTopBar"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Responsive dashboard layout switching between desktop sidebar and mobile drawer"
      exports: ["default"]
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/hooks/use-media-query.ts"
      via: "useMediaQuery hook for responsive breakpoint detection"
      pattern: "useMediaQuery.*1023px"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/app-sidebar.tsx"
      via: "Desktop sidebar rendered when not mobile"
      pattern: "AppSidebar"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/mobile-nav.tsx"
      via: "Mobile drawer rendered when mobile"
      pattern: "MobileNav"
    - from: "src/components/layout/mobile-nav.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "Sheet component for slide-out drawer"
      pattern: "Sheet.*SheetContent.*SheetTrigger"
---

<objective>
Build mobile-responsive navigation with Sheet drawer and integrate the complete responsive layout in the dashboard layout.

Purpose: Complete the responsive app shell by adding mobile navigation (Sheet drawer with full sidebar parity) and updating the dashboard layout to switch between desktop sidebar and mobile drawer at the 1024px breakpoint. This is the final wiring step that brings the entire navigation system together.

Output: Two new components (mobile nav drawer, mobile top bar) and a refactored dashboard layout that handles responsive navigation switching.
</objective>

<execution_context>
@/Users/thegoodmachine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thegoodmachine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-shell-navigation/03-RESEARCH.md
@.planning/phases/03-app-shell-navigation/03-CONTEXT.md
@.planning/phases/03-app-shell-navigation/03-01-SUMMARY.md
@src/components/layout/app-sidebar.tsx
@src/components/layout/profile-selector.tsx
@src/components/ui/sheet.tsx
@src/components/ui/button.tsx
@src/hooks/use-media-query.ts
@src/hooks/use-sidebar-state.ts
@src/app/(dashboard)/layout.tsx
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile navigation drawer and top bar</name>
  <files>
    src/components/layout/mobile-nav.tsx
    src/components/layout/mobile-top-bar.tsx
  </files>
  <action>
    **Create `src/components/layout/mobile-nav.tsx`:**
    - `'use client'` directive
    - Import: `Sheet, SheetContent, SheetHeader, SheetTitle` from `@/components/ui/sheet`
    - Import: `ScrollArea` from `@/components/ui/scroll-area`
    - Import: `ProfileSelector` from `@/components/layout/profile-selector`
    - Import: `Link` from `next/link`
    - Import: `usePathname, useRouter` from `next/navigation`
    - Import: `createClient` from `@/lib/supabase/client`
    - Import: `cn` from `@/lib/utils`
    - Import icons: `MessageSquare, Mic2, FileText, Target, BookOpen, Archive, Settings, LogOut, Zap` from `lucide-react`
    - Import types: `BrandProfile, Workspace` from `@/types`

    **Props interface:**
    ```typescript
    interface MobileNavProps {
      open: boolean
      onOpenChange: (open: boolean) => void
      workspace: Workspace | null
      profiles: BrandProfile[]
      activeProfileId: string | null
      onProfileChange: (id: string) => void
    }
    ```

    **Implementation:**
    - Controlled `Sheet` component: `open` and `onOpenChange` from props (parent controls state)
    - `SheetContent` with `side="left"` (Claude's discretion: slide from left, consistent with desktop sidebar position)
    - `SheetContent` className: `w-[280px] p-0` (slightly wider than desktop collapsed for comfortable touch targets)
    - `SheetHeader` with `className="sr-only"` for accessibility: `<SheetTitle>Navigation menu</SheetTitle>`

    **Content inside Sheet (full parity with desktop sidebar):**
    1. Workspace header: Zap icon + workspace name, with `p-4 border-b border-sidebar-border`
    2. Profile selector: `<ProfileSelector>` with `collapsed={false}` (always expanded in mobile), same props, wrapped in `p-3 border-b border-sidebar-border`
    3. Navigation items: Same `navItems` array as desktop sidebar (import or duplicate -- prefer defining locally to avoid circular deps)
       - Each item: `Link` wrapping a flex div with icon + label
       - Active detection: same `pathname === href || pathname.startsWith(href + '/')` pattern
       - Active styles: same as desktop (`bg-sidebar-accent text-sidebar-accent-foreground font-medium`)
       - IMPORTANT: Do NOT call `onOpenChange(false)` on navigation -- per user decision, navigating does NOT auto-close the drawer
    4. Sign out button at bottom: same pattern as desktop sidebar, calls `supabase.auth.signOut()` then `router.push('/login')`
    - Background: `bg-sidebar` on SheetContent for consistency with desktop

    **Named export: `MobileNav`**

    **Create `src/components/layout/mobile-top-bar.tsx`:**
    - `'use client'` directive
    - Import: `Button` from `@/components/ui/button`
    - Import: `Menu, Plus` from `lucide-react`
    - Import: `Zap` from `lucide-react`
    - Import: `Link` from `next/link`

    **Props interface:**
    ```typescript
    interface MobileTopBarProps {
      onMenuClick: () => void
    }
    ```

    **Implementation (per user decisions):**
    - Fixed top bar: `h-14 border-b border-border bg-background flex items-center justify-between px-4`
    - Left side: Hamburger button (`Menu` icon, `variant="ghost" size="icon"`) with `aria-label="Open navigation"`, calls `onMenuClick`
    - Center: Logo/brand -- `Zap` icon + "Gooder" text (or just Zap icon), `flex items-center gap-2`
    - Right side: "New Chat" shortcut button -- `Link` to `/chat` with `Plus` icon, `variant="ghost" size="icon"`, `aria-label="New chat"`
    - Per user decision: "Mobile top bar shows logo/brand only (not page title)" and "includes a New Chat shortcut button alongside the hamburger"

    **Named export: `MobileTopBar`**
  </action>
  <verify>
    1. `src/components/layout/mobile-nav.tsx` exists and exports `MobileNav`
    2. `src/components/layout/mobile-top-bar.tsx` exists and exports `MobileTopBar`
    3. MobileNav uses `Sheet` with `side="left"`
    4. MobileNav does NOT auto-close on navigation (no `onOpenChange(false)` in Link click handlers)
    5. MobileTopBar has hamburger, logo, and New Chat button
    6. `npx tsc --noEmit` passes
  </verify>
  <done>
    Mobile navigation drawer slides from left with full sidebar parity. Mobile top bar has hamburger trigger, logo, and New Chat shortcut. Drawer stays open during page navigation. TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard layout for responsive navigation switching</name>
  <files>
    src/app/(dashboard)/layout.tsx
  </files>
  <action>
    **Refactor `src/app/(dashboard)/layout.tsx`:**
    - Keep `'use client'` directive
    - Keep ALL existing data loading logic unchanged (workspace, profiles, activeProfileId, loading state, handleProfileChange, loadWorkspaceData, useEffect)
    - Add imports:
      - `useMediaQuery` from `@/hooks/use-media-query`
      - `MobileNav` from `@/components/layout/mobile-nav`
      - `MobileTopBar` from `@/components/layout/mobile-top-bar`
    - Keep existing import of `AppSidebar` from `@/components/layout/app-sidebar`

    **Add responsive state:**
    - `const isMobile = useMediaQuery('(max-width: 1023px)')` -- below lg breakpoint (1024px)
    - `const [mobileNavOpen, setMobileNavOpen] = useState(false)` for controlling mobile drawer

    **Loading state (keep existing pattern):**
    - Same spinner/loading UI, no changes needed

    **Render logic (replace only the JSX return):**

    When `isMobile` is true (mobile layout):
    ```jsx
    <div className="flex flex-col h-screen bg-background">
      <MobileTopBar onMenuClick={() => setMobileNavOpen(true)} />
      <MobileNav
        open={mobileNavOpen}
        onOpenChange={setMobileNavOpen}
        workspace={workspace}
        profiles={profiles}
        activeProfileId={activeProfileId}
        onProfileChange={handleProfileChange}
      />
      <main className="flex-1 overflow-y-auto">{children}</main>
    </div>
    ```

    When `isMobile` is false (desktop layout):
    ```jsx
    <div className="flex h-screen overflow-hidden bg-background">
      <AppSidebar
        workspace={workspace}
        profiles={profiles}
        activeProfileId={activeProfileId}
        onProfileChange={handleProfileChange}
      />
      <main className="flex-1 overflow-y-auto">{children}</main>
    </div>
    ```

    **Critical: SSR handling:**
    - The `useMediaQuery` hook returns `false` before mount (defaults to desktop)
    - This means server-rendered HTML always shows desktop layout
    - On client mount, it switches to mobile if viewport is narrow
    - This is acceptable -- the loading spinner covers the initial render anyway

    **Do NOT change:**
    - Workspace/profile data loading logic
    - handleProfileChange function
    - localStorage persistence of active profile
    - Custom event dispatch for profile changes
    - The loading state spinner UI
  </action>
  <verify>
    1. `src/app/(dashboard)/layout.tsx` imports `useMediaQuery` from `@/hooks/use-media-query`
    2. `src/app/(dashboard)/layout.tsx` imports `MobileNav` and `MobileTopBar`
    3. `src/app/(dashboard)/layout.tsx` uses ternary or conditional based on `isMobile`
    4. `src/app/(dashboard)/layout.tsx` still passes `workspace, profiles, activeProfileId, onProfileChange` to `AppSidebar`
    5. `npx tsc --noEmit` passes with zero errors
    6. `npm run build` succeeds
  </verify>
  <done>
    Dashboard layout switches between desktop sidebar and mobile Sheet drawer at 1024px breakpoint. Mobile shows top bar with hamburger + New Chat, desktop shows collapsible sidebar. All data loading logic preserved. Build passes.
  </done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` -- zero TypeScript errors
2. Run `npm run build` -- Next.js production build succeeds
3. Verify `mobile-nav.tsx` uses `Sheet` with `side="left"` and does NOT auto-close on navigation
4. Verify `mobile-top-bar.tsx` has hamburger, logo/brand, and New Chat shortcut
5. Verify `layout.tsx` uses `useMediaQuery('(max-width: 1023px)')` for responsive detection
6. Verify `layout.tsx` renders `MobileNav` + `MobileTopBar` when mobile, `AppSidebar` when desktop
7. Verify all existing data loading logic in layout.tsx is preserved (workspace, profiles, handleProfileChange)
8. Verify mobile nav has same nav items as desktop sidebar (full parity)
</verification>

<success_criteria>
- Below 1024px: mobile top bar + Sheet drawer (slide from left) replaces sidebar
- Above 1024px: collapsible desktop sidebar renders as before
- Mobile drawer has full parity: workspace header, profile selector, all nav items, sign out
- Overlay tap closes drawer, but page navigation does NOT close drawer
- Mobile top bar has hamburger (left), logo (center), New Chat shortcut (right)
- All existing workspace/profile data loading is preserved
- TypeScript clean, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-shell-navigation/03-03-SUMMARY.md`
</output>
