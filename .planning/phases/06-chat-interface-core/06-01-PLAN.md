---
phase: 06-chat-interface-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.js
  - src/app/globals.css
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03

must_haves:
  truths:
    - "react-markdown, remark-gfm, and react-shiki are installed and importable"
    - "next.config.js transpilePackages includes react-markdown and remark-gfm"
    - "globals.css contains typing-dot, message-flash, and updated streaming-cursor keyframes"
  artifacts:
    - path: "next.config.js"
      provides: "ESM transpile config for react-markdown and remark-gfm"
      contains: "transpilePackages.*react-markdown"
    - path: "src/app/globals.css"
      provides: "CSS animation keyframes for chat UI"
      contains: "typing-dot"
  key_links:
    - from: "src/app/(dashboard)/chat/markdown-message.tsx"
      to: "react-markdown"
      via: "npm import"
      pattern: "from ['\"]react-markdown['\"]"
    - from: "src/app/(dashboard)/chat/page.tsx"
      to: ".typing-dot"
      via: "className usage"
      pattern: "typing-dot"
---

<objective>
Install the three new markdown/highlighting libraries, update next.config.js for ESM compatibility, and add the three CSS animation primitives needed by the chat redesign.

Purpose: All subsequent plans (02-04) depend on these libraries being importable and these CSS classes existing. This plan has zero dependencies and runs in parallel with Plan 02.
Output: npm packages installed, next.config.js updated, globals.css expanded with chat animation keyframes.
</objective>

<execution_context>
@/Users/thegoodmachine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thegoodmachine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-interface-core/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown, remark-gfm, react-shiki</name>
  <files>package.json</files>
  <action>
    Run: `npm install react-markdown remark-gfm react-shiki`

    Expected versions per research:
    - react-markdown@^10.1.0
    - remark-gfm@^4.0.1
    - react-shiki@^0.9.1

    After install, verify all three appear in package.json dependencies.
  </action>
  <verify>
    Run `node -e "require('./node_modules/react-markdown/package.json')"` — should not throw.
    Check `package.json` for all three entries.
  </verify>
  <done>All three packages present in package.json dependencies with semver-compatible versions.</done>
</task>

<task type="auto">
  <name>Task 2: Update next.config.js for ESM transpilation</name>
  <files>next.config.js</files>
  <action>
    Read the current next.config.js. It currently has:
    ```js
    transpilePackages: ["geist"],
    ```

    Update the transpilePackages array to include react-markdown and remark-gfm:
    ```js
    transpilePackages: ["geist", "react-markdown", "remark-gfm"],
    ```

    Leave all other config options untouched (experimental.serverComponentsExternalPackages, eslint, typescript, images).

    Reason: react-markdown@10 is ESM-only. Next.js 14 requires explicit transpilePackages for ESM packages to avoid "require() of ES Module" errors in production builds.
  </action>
  <verify>
    Run `node -e "const c = require('./next.config.js'); console.log(c.transpilePackages)"` — should print array containing "react-markdown" and "remark-gfm".
  </verify>
  <done>next.config.js transpilePackages array contains ["geist", "react-markdown", "remark-gfm"] with all other config unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add chat animation keyframes to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    Read the current globals.css. It currently has:
    - `.streaming-cursor::after` using `content: "▋"` with `blink` animation
    - `@keyframes blink`
    - `@keyframes fade-in`
    - `.message-appear`

    Make the following changes:

    1. **Replace the existing `.streaming-cursor::after` block** (lines 106-110). Change from `▋` block cursor to pulsing ellipsis:
    ```css
    .streaming-cursor::after {
      content: " ...";
      animation: blink 0.8s step-end infinite;
      color: hsl(var(--muted-foreground));
    }
    ```
    Note: Keep the existing `@keyframes blink` unchanged — it already does `50% { opacity: 0; }` which works for the ellipsis too.

    2. **Add typing dot keyframe and class** after the existing `@keyframes fade-in` block:
    ```css
    @keyframes typing-dot {
      0%, 60%, 100% { transform: translateY(0); opacity: 0.4; }
      30% { transform: translateY(-5px); opacity: 1; }
    }

    .typing-dot {
      animation: typing-dot 1s ease-in-out infinite;
    }
    ```

    3. **Add message flash keyframe and class** immediately after the typing-dot block:
    ```css
    @keyframes message-flash {
      0%, 100% { background-color: transparent; }
      40% { background-color: hsl(var(--primary) / 0.06); }
    }

    .animate-message-flash {
      animation: message-flash 0.8s ease-out;
    }
    ```

    Do NOT remove or alter any other existing CSS (scrollbar styles, base layer, color variables, dark mode variables).
  </action>
  <verify>
    Read globals.css and confirm:
    - `.streaming-cursor::after` now uses `content: " ..."` (not `"▋"`)
    - `@keyframes typing-dot` exists with `30% { transform: translateY(-5px)`
    - `.typing-dot` class exists
    - `@keyframes message-flash` exists
    - `.animate-message-flash` class exists
  </verify>
  <done>globals.css contains all three animation additions; streaming-cursor updated to ellipsis; no other existing CSS removed or altered.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without ESM import errors for react-markdown or remark-gfm
2. `cat next.config.js | grep -A2 transpilePackages` shows all three packages
3. `grep -c "typing-dot\|message-flash\|streaming-cursor" src/app/globals.css` returns 4+ matches
</verification>

<success_criteria>
- react-markdown, remark-gfm, react-shiki all importable in client components
- next.config.js ESM transpilation configured (no build errors)
- CSS keyframes for typing-dot, message-flash, and updated streaming-cursor present in globals.css
- All three unblocking conditions for Plans 02 and 03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-interface-core/06-01-SUMMARY.md`
</output>
